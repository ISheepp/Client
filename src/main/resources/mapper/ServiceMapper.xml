<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!--命名空间定义-->
<mapper namespace="com.codelin.dao.NewserviceDao">

    <insert id="add" parameterType="Newservice">
        insert into newservice(id, clientname, type, outline, status, createperson, createtime, request, dealperson)
        VALUES(id=#{id}, clientname=#{clientname}, type=#{type}, outline=#{outline}, status=#{status}, createperson=#{createperson}, createtime=#{createtime}, request=#{request}, dealperson=#{dealperson})
    </insert>

    <select id="findAll" resultType="Newservice">
        select * from newservice
    </select>

</mapper>